---
title: 前端工程化面试合集
date: 2025-12-25 00:58:41
permalink: /qianduan/前端工程化
categories:
  - 前端
  - css
tags:
  - css
---

## vite和webpack的区别？
Vite 与 Webpack 的核心区别在于**开发模式原理**和**性能优化策略**：
**1.开发模式**
  - Vite：基于 ES 模块原生支持，启动时不打包，按需编译，热更新（HMR）速度极快；
  - Webpack：启动时需打包所有模块，热更新需重新编译相关模块，速度较慢。
2.生产模式
  - Vite：使用 Rollup 打包，支持 Tree Shaking、代码分割，生成的产物体积小；
  - Webpack：自身支持复杂打包逻辑，插件生态丰富，适合处理复杂项目。
3.vite默认支持热更新而不需要手动配置，webpack需要手动配置在webpack.config.js中启用devServer.hot和HotModuleReplacementPlugin。在代码中添加 module.hot.accept 逻辑。
## 为什么说 vite 比 webpack 快
vite 相比于 webpack 有依赖预购建、按需编译、以及高效的 hmr 机制、和底层语言的差异

首先 vite 在启动项目的时候不会对项目进行全部打包，会将代码分为第三方依赖和源码，第三方依赖会进行依赖预构建，将依赖尽可能的由 commonjs 转为 esm 格式，然后缓存到本地的 .vite 目录下，如果后续依赖没有发生变化，就会一直使用本地的依赖，并且 hmr(热更新)也只会针对业务代码，也就是我们的源码进行 hmr


## 什么是 Tree Shaking？
参考内容- [深入解析树摇（Tree Shaking）](https://juejin.cn/post/7435854379230543924)
树摇（Tree Shaking）是一种静态分析技术，用于消除 JavaScript 应用中的死代码。通过去除未使用的代码，它可以帮助减少打包后的文件体积，提高加载速度，优化前端应用的性能。
树摇的核心前提是 模块化，尤其是 ES6 模块（import/export）。使用 ES6 模块语法，构建工具能够更方便地进行静态分析，从而识别并删除未使用的代码。

**树摇的工作原理**

**在项目中如何启动树摇**
- webpack
Vue 项目中启用树摇，我们可以通过 vue.config.js 文件来定制 Webpack 的配置。
`usedExports: true`：启用树摇，Webpack 会通过静态分析来移除未使用的代码。
`sideEffects: false`：告诉 Webpack 该项目中没有副作用的代码，帮助工具更好地优化未使用的部分。
```js
// vue.config.js
module.exports = {
  configureWebpack: {
    optimization: {
      usedExports: true,  // 启用树摇，移除未使用的代码
      sideEffects: false, // 标记没有副作用的模块
    },
  },
};
```
- Vite项目
在 Vite 中，启用树摇几乎不需要额外配置，因为 Vite 会自动启用对静态 import 的树摇支持。
`build.rollupOptions.output.manualChunks`：通过配置手动分割外部依赖，可以进一步优化项目结构，提高性能。
```js
// vite.config.js
export default {
  build: {
    rollupOptions: {
      output: {
        manualChunks: (id) => {
          if (id.includes('node_modules')) {
            return 'vendor';  // 将外部库分割到一个单独的包中
          }
        },
      },
    },
  },
};

```
- **Vue 和 Vite 中的树摇对比**

| 特性           | Vue（Webpack）                              | Vite（ESBuild）                                 |
| -------------- | ------------------------------------------- | ----------------------------------------------- |
| **树摇支持**   | 需要通过 `vue.config.js` 显式启用           | 内置支持，不需要额外配置                        |
| **构建速度**   | 较慢，依赖 Webpack 的构建速度               | 极快，基于 ESBuild 构建，速度非常快             |
| **模块化支持** | 支持 ES6 模块化，推荐使用 `import`/`export` | 天生支持 ES6 模块化，支持动态导入（`import()`） |
| **副作用处理** | 可通过 `sideEffects` 配置优化               | 默认优化，自动识别副作用                        |


## Vite为什么比Webpack本地构建速度快？
webpack需要分析整个依赖图，进行多次文件扫描和转译。vite只构建正在编辑的文件，而不是整个项目。


## 热更新是指什么？
Hot Module Replacement（模块热更新），简称 HMR，是指在应用程序运行过程中，替换、添加或删除模块，而无需重新加载整个页面。只需要局部刷新页面上发生变化的模块，同时可以保留当前的页面状态，比如复选框的选中状态、输入框的输入等。


## Babel的原理是什么?
Babel 是 JavaScript 的编译器，用于将 ES6+ 等新语法转换为向后兼容的 ES5 代码，同时支持语法扩展和代码转换。
babel 的转译过程也分为三个阶段，这三步具体是：
**解析 - 转换 - 生成**

1.**解析（Parse）**：将源代码解析为抽象语法树（AST），包括词法分析和语法分析；
2.**转换（Transform）**：遍历 AST，通过插件修改节点，实现语法转换（如 ES6→ES5）、代码优化；
3.**生成（Generate）**：将修改后的 AST 转换回 JavaScript 代码；
