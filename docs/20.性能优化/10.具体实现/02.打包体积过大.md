---
title: 打包优化
date: 2025-11-27 17:24:35
permalink: /performance/打包优化
# titleTag: 
categories:
  - 性能优化
tags:
  - 性能优化
---
> **参考内容**
> [Vite打包优化实践：从分包到性能提升](https://juejin.cn/post/7480534286514749459)

在优化前，首先要知道打包产物都有什么，哪些文件体积大。可以通过使用`rollup-plugin-visualizer`可视化分析。

::: details 💡使用方法
第一步:安装依赖包
```js
pnpm add rollup-plugin-visualizer -D
```
第二步:在`vite.config.ts`内配置
```js
import { visualizer } from "rollup-plugin-visualizer";

export default defineConfig({
  plugins: [vue(), visualizer({ open: true })],
});
```
:::

下面就是介绍常使用的几种打包优化手段，按需加载、剔除无用代码(Tree Shaking)、压缩与混淆、图片优化、分包策略

## 1.按需加载
常使用方法:路由懒加载、UI库按需引入等

### 路由懒加载

**概念**:路由懒加载是一种优化手段，它将路由对应的组件代码分割成独立的 chunk 文件，在用户访问该路由时才进行加载，而不是在应用初始加载时一次性加载所有组件。

**原理**:借助代码分割（Code Splitting）技术，如 Webpack 的动态 import（import()）语法，将不同路由的组件代码分离出来，形成多个较小的 bundle 文件。当路由被访问时，通过异步请求加载对应的 bundle 文件并渲染组件。

```js
//将import UserDetails from './views/UserDetails.vue'替换成下面方式
const UserDetails = () => import('./views/UserDetails.vue')

//或者在路由定义中直接使用
const router = createRouter({
  routes:{
    { path: '/users/:id', component: () => import('./views/UserDetails.vue') }
  }
})
```

### UI库按需引入

