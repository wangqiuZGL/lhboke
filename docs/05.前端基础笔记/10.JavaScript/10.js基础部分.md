---
title: js基础部分
date: 2025-11-28 17:58:41
permalink: /qianduan/基础
categories:
  - 前端
  - javascript
tags:
  - javascript
---

<CollapsibleToc 
  title="快速导航" 
  :defaultOpen="true" 
  :minLevel="2" 
  :maxLevel="3" 
/>

::: details 💡 内容大纲

- 理解DOM操作
- 事件处理、事件冒泡、事件委托
- JS有多少种继承方式

:::




## settimeout和setIntercal的陷阱
::: details 💡 内容详情

`setTimeout`和`setInterval`是 Window/Worker 接口提供的定时器API

**`setTimeout:`** 迟执行一次（一次性定时器）
- 作用:设置一个「延迟时间」，到达时间后仅执行一次指定的函数/代码片段。
- 延迟执行非紧急任务、模拟异步操作、防抖
**`setInterval:`** 重复执行（周期性定时器）
- 周期性更新数据、轮播图自动切换、实时监控

::: code-group
```js [settimeout实现防抖]
let searchTimer;
input.addEventListener("input", (e) => {
  clearTimeout(searchTimer); // 清除上一次的定时器
  searchTimer = setTimeout(() => {
    fetchSearch(e.target.value); // 输入停止500ms后执行搜索
  }, 500);
});
```

``` js [setinterval]
// 60秒倒计时
let seconds = 60;
const timer = setInterval(() => {
  seconds--;
  if (seconds <= 0) {
    clearInterval(timer);
    console.log("倒计时结束");
  }
  console.log(`剩余：${seconds}秒`);
}, 1000);
```
:::


## JS中实现继承
::: details 💡 继承的目标

- 属性私有化:子类实例的属性不共享
- 方法复用:父类的方法挂载到原型上，所有子类实例共享
- 传参灵活:子类能向父类构造函数传递参数；
- 原型链干净:子类原型修改不影响父类原型，无多余属性。

:::
### 在ES6中通过使用class继承
先看一个基础的类一般包含什么。
```js
class Person {
  // 1. 公有属性,声明实例属性 name（初始值默认 undefined）
  name
  // 2. 构造函数
  constructor(name) {
    this.name = name
  }
  // 3. 公有方法
  sayHi() {
    console.log(`你好,我叫:${this.name}`)
  }
}
```
- 继承的方法extends:继承父类
- super：一般用来调用父类的构造函数,如果调用父类的构造函数，必须要使用

```js
class Students extends Person{
    age;
    constructor(name, age){
      super(name); //调用父类的构造函数
      this.age = age;
    }
    sayAge() {
      console.log(`我今年${this.age}岁`);
    }
  }
  const s = new Students('小明',20);
```

除此之外js应该也是借鉴c++的方式，在类里面定义静态属性和私有属性等。静态属性可以在外面调用，私有属性只能在类里面使用。
```js
//在属性和方法前加入static就是静态的
static name='静态属性'
//在属性和方法前加入#就是私有的
#Info='我是私有属性'
```


### 在ES5中使用寄生组合式继承
通过借用构造函数来继承属性, 在原型上添加共用的方法, 通过寄生式实现继承.**构造函数来继承属性，通过原型链继承方法**
::: details 💡 ES6 Class extends 继承
ES6 新增的 class 和 extends 是「寄生组合式继承」的语法糖，底层逻辑和寄生组合式完全一致，但写法更接近传统面向对象。
:::
```js
//首先是父类
function Person(name) {
  this.name = name;
}
//给父类添加方法
Person.prototype.sayHi = function() {
  console.log(`你好,我叫:${this.name}`);
}


//----------------子类通过寄生式组合------------------
//1.通过构建函数继承属性
function Students(name) {
  Person.call(this, name); //调用父类的构造函数
}
//2.通过原型的方式继承方法
Students.prototype = Object.create(Persoprototype);
//本来constructor指向Person,需要修正指向Students
Students.prototype.constructor = Students;

const s = new Students('小明');
```
